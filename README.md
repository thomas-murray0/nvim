Minimalist Neovim Config
using version Nightly

Todo:
    - Play around with telescope themes
    - Implement snippets from old config, but remove snippets from blink unless enabled
        - maybe add an optional telescope search snippets functionality
    - Look into what telescope.builtin.code_actions are??? Could be lucrative
    - Add custom telescope binding to search in another directory, should first ask to type in dir path from $HOME or if specified /bin/...
